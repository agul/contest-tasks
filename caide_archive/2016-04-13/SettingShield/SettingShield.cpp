#include "Head.h"

const int MOD = 1000000007;
const int MAXN = 111111;


class SettingShield {
public:
    /// caide keep
    long long getOptimalCost(int n, int h, int t, vector<int> val0, vector<int> a, vector<int> b, vector<int> m) {
        long long result = 0;
        return result;
    }
};


long long solve(int n, int h, int t, vector<int> val0, vector<int> a, vector<int> b, vector<int> m, int) {
    SettingShield sol;
    return sol.getOptimalCost(n, h, t, val0, a, b, m);
}
