#include "Head.h"

const int MOD = 1000000007;
const int MAXN = 111111;


class CliqueCuts {
public:
    /// caide keep
    int sum(int n, vector<int> a, vector<int> b, vector<int> c) {
        int result = 0;
        return result;
    }
};


int solve(int n, vector<int> a, vector<int> b, vector<int> c, int) {
    CliqueCuts sol;
    return sol.sum(n, a, b, c);
}
