#include "Head.h"

const int MOD = 1000000007;
const int MAXN = 111111;


class BearGridRect {
public:
    /// caide keep
    vector<int> findPermutation(int N, vector<int> r1, vector<int> r2, vector<int> c1, vector<int> c2, vector<int> cnt) {
        vector<int> result;
        return result;
    }
};


vector<int> solve(int N, vector<int> r1, vector<int> r2, vector<int> c1, vector<int> c2, vector<int> cnt, int) {
    BearGridRect sol;
    return sol.findPermutation(N, r1, r2, c1, c2, cnt);
}
